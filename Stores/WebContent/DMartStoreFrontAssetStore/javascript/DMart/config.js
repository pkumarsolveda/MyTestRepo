// All UI configuration define here.

window.DM_UI_CONFIG = {};

window.DM_UI_CONFIG.defaultTransition = 0.2;

// Protect your code with this wrapper function

// (function(dmUIConfig) {
  // Your code goes here.
  // You can access the UI config using `dmUIConfig`
// }(window.DM_UI_CONFIG));
